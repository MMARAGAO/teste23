/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // isso permite que você controle o dark mode adicionando uma classe ao elemento raiz
  content: ["./src/**/*.{html,js}"],
  theme: {
    extend: {},
  },
  plugins: [],
}